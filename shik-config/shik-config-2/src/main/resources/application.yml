server:
  port: 8882

spring:
  application:
    name: shik-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zkgengkun/shik/
          searchPaths: shik-common/config-repo
          username: username
          password: password
      discovery:
        enabled: true
  rabbitmq:
    addresses: 127.0.0.1:5672
    username: root
    password: qweASDzxc

security:
  user:
    name: shik
    password: shik-config-server

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:1111/eureka/,http://localhost:1112/eureka/,http://localhost:1113/eureka/
